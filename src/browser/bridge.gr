module Bridge

from "../core/analysis.gr" include Analysis
from "../frontend/frontend.gr" include Frontend
from "./serialize.gr" include Serialize

provide let compileToJson = (source: String) => {
  let loweredProgram = Frontend.parseAndLower(source)
  let analysis = Analysis.analyze(source)
  Serialize.analysisToJson(analysis)
}
