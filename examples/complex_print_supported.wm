let inc = (x) => {
  x + 1
};

let mul2 = (x) => {
  x * 2
};

let main = => {
  let base = 10;
  let a = inc(base);
  let local = () => {
    a * 3
  };
  let b = local() + mul2(a);
  let c = if (true) {
    b + 5
  } else {
    b - 5
  };
  print(c)
};