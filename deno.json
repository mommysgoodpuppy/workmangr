{
  "tasks": {
    "test:notparallel": "grain compile submodules/graintest/lib/runner.gr -o runner.wasm && wasmtime --dir . runner.wasm -- --batch | pwsh -NoProfile -NonInteractive -Command '$content = [Console]::In.ReadToEnd(); Invoke-Expression $content'",
    "test": "deno run --allow-read --allow-run --allow-env scripts/test_parallel.ts",
    "test:wmc-conformance": "deno run --allow-run scripts/test_wmc_conformance.ts",
    "test:unix": "grain compile lib/runner.gr -o runner.wasm && wasmtime --dir . runner.wasm -- --shell ./tests | sh",
    "test:discover": "wasmtime --dir . runner.wasm -- ./tests",
    "clean": "deno run -A scripts/clean_grain_cache.ts",
    "docs:reference": "deno run --allow-read --allow-write docs/reference/combine_md.ts"
  }
}
